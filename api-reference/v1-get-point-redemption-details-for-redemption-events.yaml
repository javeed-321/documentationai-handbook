openapi: 3.1.0
info:
  title: Get Point Redemption Details for Redemption Events
  version: '1.0'
servers:
- url: "https://{host}"
  variables:
    host:
      default: host
security:
- sec0: []
paths:
  /das/getPointsRedemptionExplodeLedger:
    get:
      summary: Get Point Redemption Details for Redemption Events
      description: ''
      operationId: get-point-redemption-details-for-redemption-events
      parameters:
      - name: entity_id
        in: query
        description: Customer ID of the customer.
        required: true
        schema:
          type: integer
          format: int64
      - name: event_log_ids
        in: query
        description: Unique identifier generated for the event at the time of its
          creation.
        required: true
        schema:
          type: string
      - name: count
        in: query
        description: Number of results to display per page. The default value is 10.
          The maximum supported value is 100.
        schema:
          type: integer
          format: int32
      - name: page
        in: query
        description: "Retrieve details of a specific page. Page count starts from\
          \ 1.  Example: page=2 retrieves data on page 2."
        schema:
          type: integer
          format: int32
      responses:
        '200':
          description: '200'
          content:
            application/json:
              examples:
                Sample Response:
                  value: ''
        '400':
          description: '400'
          content:
            application/json:
              examples:
                Result:
                  value: "{}"
              schema:
                type: object
                properties: {}
      deprecated: false
components:
  securitySchemes:
    sec0:
      type: http
      scheme: basic
